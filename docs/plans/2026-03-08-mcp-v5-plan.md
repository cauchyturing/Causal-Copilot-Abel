# MCP Server v0.3 — Implementation Plan (v5, GPT Consensus)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Transform Causal-Copilot into a production MCP server — any LLM becomes a causal discovery expert. 6 tools, 55+ resources, 2 prompts, three Docker images, offline-capable.

**Architecture:** Bridge layer connects FastMCP 3.x server to existing legacy pipeline (28+ algorithms, full LLM integration, causal inference). Phase 0 hardens the pipeline's statistical methodology first. The existing `causal_copilot/mcp/server.py` (3 tools) is evolved to 6 tools with full pipeline access.

**Tech Stack:** FastMCP 3.x, Python 3.10+, pytest, existing pipeline modules

**Starting point:** Current repo state — `causal_copilot/mcp/` exists with 3 basic tools. Legacy pipeline (`causal_discovery/`, `preprocess/`, `postprocess/`, `causal_inference/`, `llm/`, `global_setting/`) fully functional. 28 algorithm wrappers. Docker + LLM integration done.

**GPT consensus reference:** `docs/plans/2026-03-08-gpt-discussion-mcp.md`

---

## Phase 0: Pipeline Hardening

Fix scientific gaps before wrapping as MCP. An "expert system" that picks fisherz for nonlinear data isn't expert.

---

### Task 1: CI Test Resolver

**Files:**
- Create: `causal_discovery/ci_test_resolver.py`
- Test: `tests/test_ci_test_resolver.py`

**Step 1: Write failing tests**

```python
# tests/test_ci_test_resolver.py
"""Tests for deterministic CI test resolution."""
import pytest
from unittest.mock import MagicMock


def _make_stats(**kwargs):
    """Create a mock Statistics object with given attributes."""
    defaults = {
        "missingness": False,
        "data_type": "Continuous",
        "linearity": True,
        "gaussian_error": True,
        "sample_size": 500,
        "feature_number": 10,
        "time_series": False,
    }
    defaults.update(kwargs)
    s = MagicMock()
    for k, v in defaults.items():
        setattr(s, k, v)
    return s


class TestCITestResolver:
    def test_missing_data_returns_mv_fisherz(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(missingness=True)
        assert resolve_ci_test(stats) == "mv_fisherz"

    def test_discrete_returns_chisq(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(data_type="Discrete")
        assert resolve_ci_test(stats) == "chisq"

    def test_linear_continuous_returns_fisherz(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(linearity=True, data_type="Continuous")
        assert resolve_ci_test(stats) == "fisherz"

    def test_nonlinear_small_returns_kci(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(linearity=False, sample_size=500, feature_number=8)
        assert resolve_ci_test(stats) == "kci"

    def test_nonlinear_medium_returns_rcit(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(linearity=False, sample_size=5000, feature_number=50)
        assert resolve_ci_test(stats) == "rcit"

    def test_nonlinear_large_returns_fastkci(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(linearity=False, sample_size=20000, feature_number=150)
        assert resolve_ci_test(stats) == "fastkci"

    def test_binary_returns_chisq(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_stats(data_type="Binary")
        assert resolve_ci_test(stats) == "chisq"
```

**Step 2: Run tests to verify they fail**

Run: `cd /home/bht/Abel/Causal-Copilot-Abel && python -m pytest tests/test_ci_test_resolver.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'causal_discovery.ci_test_resolver'`

**Step 3: Write implementation**

```python
# causal_discovery/ci_test_resolver.py
"""Deterministic CI test resolution based on data characteristics.

Replaces LLM guesswork with data-driven CI test selection.
Hierarchy: missing → discrete → linear → nonlinear (size-based).
"""


def resolve_ci_test(statistics):
    """Select CI test based on data characteristics.

    Args:
        statistics: GlobalState.statistics with linearity, data_type,
                   missingness, sample_size, feature_number attributes.

    Returns:
        str: CI test name compatible with causal-learn.
    """
    if statistics.missingness:
        return "mv_fisherz"

    if statistics.data_type in ("Discrete", "Category", "Binary"):
        return "chisq"

    if statistics.linearity:
        return "fisherz"

    # Nonlinear: pick based on computational budget
    n = statistics.sample_size or 0
    p = statistics.feature_number or 0

    if n < 1500 and p < 10:
        return "kci"
    if n < 10000 and p < 100:
        return "rcit"
    return "fastkci"
```

**Step 4: Run tests to verify they pass**

Run: `cd /home/bht/Abel/Causal-Copilot-Abel && python -m pytest tests/test_ci_test_resolver.py -v`
Expected: 7 PASSED

**Step 5: Commit**

```bash
git add causal_discovery/ci_test_resolver.py tests/test_ci_test_resolver.py
git commit -m "feat: add deterministic CI test resolver

Data-driven CI test selection: missing→mv_fisherz, discrete→chisq,
linear→fisherz, nonlinear→kci/rcit/fastkci based on dataset size."
```

---

### Task 2: Score Function Resolver

**Files:**
- Create: `causal_discovery/score_resolver.py`
- Test: `tests/test_score_resolver.py`

**Step 1: Write failing tests**

```python
# tests/test_score_resolver.py
"""Tests for deterministic score function resolution."""
from unittest.mock import MagicMock


def _make_stats(**kwargs):
    defaults = {
        "data_type": "Continuous",
        "linearity": True,
    }
    defaults.update(kwargs)
    s = MagicMock()
    for k, v in defaults.items():
        setattr(s, k, v)
    return s


class TestScoreResolver:
    def test_discrete_returns_bdeu(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_stats(data_type="Discrete")
        assert resolve_score_func(stats, "GES") == "local_score_BDeu"

    def test_nonlinear_returns_cv_general(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_stats(linearity=False, data_type="Continuous")
        assert resolve_score_func(stats, "GES") == "local_score_CV_general"

    def test_linear_ges_returns_bic(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_stats(linearity=True)
        assert resolve_score_func(stats, "GES") == "local_score_BIC"

    def test_linear_grasp_returns_bic_from_cov(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_stats(linearity=True)
        assert resolve_score_func(stats, "GRaSP") == "local_score_BIC_from_cov"

    def test_category_returns_bdeu(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_stats(data_type="Category")
        assert resolve_score_func(stats, "FGES") == "local_score_BDeu"
```

**Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_score_resolver.py -v`
Expected: FAIL — ModuleNotFoundError

**Step 3: Write implementation**

```python
# causal_discovery/score_resolver.py
"""Deterministic score function resolution based on data characteristics.

Maps data type + linearity to appropriate score function for score-based algorithms.
"""


def resolve_score_func(statistics, algorithm):
    """Select score function based on data characteristics.

    Args:
        statistics: GlobalState.statistics with data_type, linearity.
        algorithm: Algorithm name (str). GRaSP uses BIC_from_cov variant.

    Returns:
        str: Score function name compatible with causal-learn.
    """
    if statistics.data_type in ("Discrete", "Category", "Binary"):
        return "local_score_BDeu"

    if not statistics.linearity:
        return "local_score_CV_general"

    if algorithm == "GRaSP":
        return "local_score_BIC_from_cov"

    return "local_score_BIC"
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_score_resolver.py -v`
Expected: 5 PASSED

**Step 5: Commit**

```bash
git add causal_discovery/score_resolver.py tests/test_score_resolver.py
git commit -m "feat: add deterministic score function resolver

discrete→BDeu, nonlinear→CV_general, linear→BIC (GRaSP→BIC_from_cov)."
```

---

### Task 3: PDAG Policy Module

**Files:**
- Create: `causal_discovery/pdag_policy.py`
- Test: `tests/test_pdag_policy.py`

**Step 1: Write failing tests**

```python
# tests/test_pdag_policy.py
"""Tests for PDAG→inference policy."""
import numpy as np
import pytest


class TestClassifyGraphKind:
    def test_dag_all_directed(self):
        from causal_discovery.pdag_policy import classify_graph_kind
        # X→Y→Z (all directed, no undirected)
        adj = np.array([[0, 0, 0], [1, 0, 0], [0, 1, 0]])
        assert classify_graph_kind(adj) == "dag"

    def test_cpdag_has_undirected(self):
        from causal_discovery.pdag_policy import classify_graph_kind
        # X→Y, Y—Z (undirected edge = 2)
        adj = np.array([[0, 0, 0], [1, 0, 2], [0, 2, 0]])
        assert classify_graph_kind(adj) == "cpdag"

    def test_pag_has_circle_marks(self):
        from causal_discovery.pdag_policy import classify_graph_kind
        # PAG edge types: 4=circle-tail, 5=circle-arrow, etc.
        adj = np.array([[0, 4], [5, 0]])
        assert classify_graph_kind(adj) == "pag"

    def test_empty_graph(self):
        from causal_discovery.pdag_policy import classify_graph_kind
        adj = np.zeros((3, 3))
        assert classify_graph_kind(adj) == "dag"  # trivially a DAG


class TestInferencePolicy:
    def test_dag_allows_full_inference(self):
        from causal_discovery.pdag_policy import check_inference_policy
        adj = np.array([[0, 0], [1, 0]])  # X→Y
        result = check_inference_policy(adj, is_linear_gaussian=True)
        assert result["allow_inference"] is True
        assert result["method"] == "standard"

    def test_cpdag_linear_gaussian_allows_ida(self):
        from causal_discovery.pdag_policy import check_inference_policy
        adj = np.array([[0, 2], [2, 0]])  # X—Y undirected
        result = check_inference_policy(adj, is_linear_gaussian=True)
        assert result["allow_inference"] is True
        assert result["method"] == "ida"

    def test_cpdag_nonlinear_rejects(self):
        from causal_discovery.pdag_policy import check_inference_policy
        adj = np.array([[0, 2], [2, 0]])
        result = check_inference_policy(adj, is_linear_gaussian=False)
        assert result["allow_inference"] is False
        assert "ambiguous" in result["reason"]

    def test_pag_always_rejects(self):
        from causal_discovery.pdag_policy import check_inference_policy
        adj = np.array([[0, 4], [5, 0]])
        result = check_inference_policy(adj, is_linear_gaussian=True)
        assert result["allow_inference"] is False
        assert "PAG" in result["reason"]

    def test_identifiable_edges(self):
        from causal_discovery.pdag_policy import get_identifiable_edges
        # X→Y (directed=identifiable), Y—Z (undirected=ambiguous)
        adj = np.array([[0, 0, 0], [1, 0, 2], [0, 2, 0]])
        names = ["X", "Y", "Z"]
        ident = get_identifiable_edges(adj, names)
        assert {"from": "X", "to": "Y"} in ident["identifiable"]
        assert any("Y" in e["nodes"] and "Z" in e["nodes"] for e in ident["ambiguous"])
```

**Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_pdag_policy.py -v`
Expected: FAIL

**Step 3: Write implementation**

```python
# causal_discovery/pdag_policy.py
"""PDAG policy: what inference is valid for each graph type.

- DAG: full inference (all edges identified)
- CPDAG: IDA when linear-Gaussian, else reject ambiguous edges
- PAG: never coerce to DAG, reject inference on ambiguous edges

This prevents the scientific error of treating undirected edges as directed.
"""
import numpy as np


# Edge type constants (from causal-learn convention)
_DIRECTED = 1
_UNDIRECTED = 2
_BIDIRECTED = 3
_PAG_TYPES = {4, 5, 6, 7}  # circle-tail, circle-arrow, etc.


def classify_graph_kind(adj):
    """Classify adjacency matrix as dag, cpdag, or pag.

    Args:
        adj: numpy array, adjacency matrix (mat[i,j] convention: j→i)

    Returns:
        str: 'dag', 'cpdag', or 'pag'
    """
    values = set(np.unique(adj).astype(int)) - {0}
    if values & _PAG_TYPES:
        return "pag"
    if _UNDIRECTED in values or _BIDIRECTED in values:
        return "cpdag"
    return "dag"


def check_inference_policy(adj, is_linear_gaussian=False):
    """Determine what causal inference is valid for this graph.

    Args:
        adj: adjacency matrix
        is_linear_gaussian: True if data is linear with Gaussian errors

    Returns:
        dict with keys:
            allow_inference (bool): whether inference is valid
            method (str): 'standard', 'ida', or None
            reason (str): explanation
            graph_kind (str): 'dag', 'cpdag', 'pag'
    """
    kind = classify_graph_kind(adj)

    if kind == "dag":
        return {
            "allow_inference": True,
            "method": "standard",
            "reason": "All edges directed — full causal inference valid.",
            "graph_kind": kind,
        }

    if kind == "cpdag":
        if is_linear_gaussian:
            return {
                "allow_inference": True,
                "method": "ida",
                "reason": "CPDAG with linear-Gaussian data — IDA provides valid bounds.",
                "graph_kind": kind,
            }
        return {
            "allow_inference": False,
            "method": None,
            "reason": "CPDAG with ambiguous edges — cannot identify causal effects "
                      "without linear-Gaussian assumption.",
            "graph_kind": kind,
        }

    # PAG
    return {
        "allow_inference": False,
        "method": None,
        "reason": "PAG output — latent confounders possible. "
                  "Cannot coerce to DAG for inference.",
        "graph_kind": kind,
    }


def get_identifiable_edges(adj, node_names):
    """Partition edges into identifiable and ambiguous.

    Args:
        adj: adjacency matrix
        node_names: list of variable names

    Returns:
        dict with 'identifiable' (directed edges) and 'ambiguous' (undirected/PAG)
    """
    n = len(node_names)
    identifiable = []
    ambiguous = []

    for i in range(n):
        for j in range(i + 1, n):
            if adj[i, j] == 0 and adj[j, i] == 0:
                continue
            # Check for directed edge: j→i means adj[i,j]=1, adj[j,i]=0
            if adj[i, j] == _DIRECTED and adj[j, i] == 0:
                identifiable.append({"from": node_names[j], "to": node_names[i]})
            elif adj[j, i] == _DIRECTED and adj[i, j] == 0:
                identifiable.append({"from": node_names[i], "to": node_names[j]})
            else:
                ambiguous.append({"nodes": [node_names[i], node_names[j]],
                                  "type": "undirected" if adj[i, j] == _UNDIRECTED else "pag"})

    return {"identifiable": identifiable, "ambiguous": ambiguous}
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_pdag_policy.py -v`
Expected: 9 PASSED

**Step 5: Commit**

```bash
git add causal_discovery/pdag_policy.py tests/test_pdag_policy.py
git commit -m "feat: add PDAG policy module

DAG→full inference, CPDAG→IDA when linear-Gaussian else reject,
PAG→never coerce. Includes edge identifiability classification."
```

---

### Task 4: Wire Resolvers + Missing Data Policy

**Files:**
- Modify: `causal_discovery/hyperparameter_selector.py` (add resolver calls after LLM HP selection)
- Test: `tests/test_resolver_integration.py`

**Step 1: Write integration test**

```python
# tests/test_resolver_integration.py
"""Integration tests for resolver wiring into HP selector."""
from unittest.mock import MagicMock, patch
import pytest


def _make_global_state(linearity=True, data_type="Continuous", missingness=False,
                       sample_size=500, feature_number=10, algorithm="PC"):
    gs = MagicMock()
    gs.statistics.linearity = linearity
    gs.statistics.data_type = data_type
    gs.statistics.missingness = missingness
    gs.statistics.sample_size = sample_size
    gs.statistics.feature_number = feature_number
    gs.statistics.gaussian_error = linearity  # simplification
    gs.statistics.time_series = False
    gs.algorithm.selected_algorithm = algorithm
    gs.algorithm.algorithm_arguments = {"indep_test": "fisherz"}  # LLM default
    return gs


class TestResolverOverride:
    def test_nonlinear_pc_overrides_to_kci(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_global_state(linearity=False, sample_size=500, feature_number=5).statistics
        result = resolve_ci_test(stats)
        assert result == "kci"

    def test_discrete_ges_overrides_to_bdeu(self):
        from causal_discovery.score_resolver import resolve_score_func
        stats = _make_global_state(data_type="Discrete").statistics
        result = resolve_score_func(stats, "GES")
        assert result == "local_score_BDeu"

    def test_missing_data_overrides_to_mv_fisherz(self):
        from causal_discovery.ci_test_resolver import resolve_ci_test
        stats = _make_global_state(missingness=True).statistics
        result = resolve_ci_test(stats)
        assert result == "mv_fisherz"
```

**Step 2: Run tests**

Run: `python -m pytest tests/test_resolver_integration.py -v`
Expected: 3 PASSED (resolvers already work standalone)

**Step 3: Modify hyperparameter_selector.py**

Read `causal_discovery/hyperparameter_selector.py` first. After the LLM generates `algorithm_arguments`, add resolver override. Find where `global_state.algorithm.algorithm_arguments` is set and add after it:

```python
# Add at the end of forward() method, after algorithm_arguments is set:

# Override CI test with deterministic resolver
from causal_discovery.ci_test_resolver import resolve_ci_test
from causal_discovery.score_resolver import resolve_score_func

algo_args = global_state.algorithm.algorithm_arguments or {}
algo_name = global_state.algorithm.selected_algorithm

# CI test override for constraint-based algorithms
ci_test_algos = {"PC", "FCI", "CDNOD", "PCParallel", "InterIAMB",
                 "BAMB", "HITONMB", "IAMBnPC", "MBOR"}
if algo_name in ci_test_algos and "indep_test" in algo_args:
    algo_args["indep_test"] = resolve_ci_test(global_state.statistics)

# Score function override for score-based algorithms
score_algos = {"GES", "FGES", "XGES", "GRaSP", "ExactSearch", "BOSS"}
if algo_name in score_algos and "score_func" in algo_args:
    algo_args["score_func"] = resolve_score_func(
        global_state.statistics, algo_name
    )

# Missing data policy: PC + missing → mvpc=True
if algo_name == "PC" and global_state.statistics.missingness:
    algo_args["mvpc"] = True

global_state.algorithm.algorithm_arguments = algo_args
```

**Step 4: Run all resolver tests**

Run: `python -m pytest tests/test_ci_test_resolver.py tests/test_score_resolver.py tests/test_resolver_integration.py -v`
Expected: 15 PASSED

**Step 5: Commit**

```bash
git add causal_discovery/hyperparameter_selector.py tests/test_resolver_integration.py
git commit -m "feat: wire CI/score resolvers into HP selector

Override LLM HP choices with deterministic resolvers. Missing data
policy: PC + missing → mvpc=True + mv_fisherz."
```

---

## Phase 1: MCP Server Enhancement

Evolve existing `causal_copilot/mcp/server.py` (3 tools → 6 tools). Add bridge to legacy pipeline.

---

### Task 5: Bridge Module

**Files:**
- Create: `causal_copilot/mcp/bridge.py`
- Test: `tests/test_mcp_bridge.py`

**Step 1: Write failing tests**

```python
# tests/test_mcp_bridge.py
"""Tests for the MCP-to-pipeline bridge."""
import pytest
import numpy as np
import pandas as pd


class TestBridgeImports:
    def test_setup_pipeline_adds_paths(self):
        from causal_copilot.mcp.bridge import PIPELINE_ROOT
        import sys
        assert str(PIPELINE_ROOT) in sys.path

    def test_can_import_global_state(self):
        from causal_copilot.mcp.bridge import make_global_state
        assert callable(make_global_state)


class TestMakeGlobalState:
    def test_from_dataframe(self):
        from causal_copilot.mcp.bridge import make_global_state
        df = pd.DataFrame({"x": [1, 2, 3], "y": [4, 5, 6]})
        gs = make_global_state(df)
        assert gs.user_data.raw_data is not None
        assert gs.user_data.selected_features == ["x", "y"]

    def test_with_query(self):
        from causal_copilot.mcp.bridge import make_global_state
        df = pd.DataFrame({"x": [1, 2, 3], "y": [4, 5, 6]})
        gs = make_global_state(df, query="What causes y?")
        assert "y" in gs.user_data.initial_query
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp_bridge.py -v`
Expected: FAIL

**Step 3: Write bridge implementation**

```python
# causal_copilot/mcp/bridge.py
"""Bridge between MCP server and legacy Causal-Copilot pipeline.

Handles sys.path setup, CWD management, and GlobalState construction
so the MCP server can call pipeline modules directly.
"""
from __future__ import annotations

import os
import sys
import tempfile
from pathlib import Path
from types import SimpleNamespace

import numpy as np
import pandas as pd

# Pipeline root = project root (two levels up from this file)
PIPELINE_ROOT = Path(__file__).resolve().parent.parent.parent

# Add pipeline modules to sys.path
_paths = [
    str(PIPELINE_ROOT),
    str(PIPELINE_ROOT / "externals"),
    str(PIPELINE_ROOT / "externals" / "causal-learn"),
]
for p in _paths:
    if p not in sys.path:
        sys.path.insert(0, p)

# Now pipeline imports work
from global_setting.state import GlobalState  # noqa: E402


def make_global_state(df, query="", algorithm=None, seed=42):
    """Construct GlobalState from a DataFrame.

    Args:
        df: pandas DataFrame with the data
        query: optional causal question
        algorithm: optional algorithm name to force
        seed: random seed

    Returns:
        Initialized GlobalState ready for pipeline stages.
    """
    output_dir = tempfile.mkdtemp(prefix="cc_mcp_")

    gs = GlobalState()
    gs.user_data.raw_data = df.copy()
    gs.user_data.processed_data = df.copy()
    gs.user_data.initial_query = query or "Discover causal relationships in this dataset."
    gs.user_data.selected_features = df.columns.tolist()
    gs.user_data.output_report_dir = output_dir
    gs.user_data.output_graph_dir = output_dir

    if algorithm:
        gs.algorithm.selected_algorithm = algorithm

    return gs


def make_args(query="", seed=42, debug=False):
    """Create args namespace compatible with pipeline modules.

    Returns:
        SimpleNamespace mimicking argparse output for main.py.
    """
    return SimpleNamespace(
        data_file="<mcp>",
        output_report_dir=tempfile.mkdtemp(prefix="cc_mcp_"),
        output_graph_dir=tempfile.mkdtemp(prefix="cc_mcp_"),
        simulation_mode="offline",
        data_mode="real",
        debug=debug,
        initial_query=query or "Discover causal relationships.",
        parallel=False,
        demo_mode=False,
    )


def adj_to_edges(adj, node_names):
    """Convert adjacency matrix to edge list.

    Args:
        adj: numpy array, mat[i,j]=1 means j→i
        node_names: list of variable names

    Returns:
        list of dicts with from, to, type, value
    """
    edges = []
    n = len(node_names)
    edge_types = {1: "directed", 2: "undirected", 3: "bidirected"}

    for i in range(n):
        for j in range(n):
            v = int(adj[i, j])
            if v == 0:
                continue
            if v == 1:  # j→i
                edges.append({
                    "from": node_names[j],
                    "to": node_names[i],
                    "type": "directed",
                })
            elif v == 2 and i < j:  # undirected, emit once
                edges.append({
                    "from": node_names[i],
                    "to": node_names[j],
                    "type": "undirected",
                })
            elif v == 3 and i < j:  # bidirected, emit once
                edges.append({
                    "from": node_names[i],
                    "to": node_names[j],
                    "type": "bidirected",
                })

    return edges


def serialize_result(gs, node_names=None, provenance=None):
    """Serialize GlobalState results to JSON-compatible dict.

    Args:
        gs: GlobalState after pipeline execution
        node_names: list of variable names
        provenance: dict with algorithm, seed, runtime, etc.

    Returns:
        dict ready for JSON serialization
    """
    adj = gs.results.converted_graph
    if adj is None:
        adj = gs.results.raw_result
    if adj is None:
        return {"status": "error", "error": "No graph produced"}

    if node_names is None:
        node_names = gs.user_data.selected_features or [
            f"V{i}" for i in range(adj.shape[0])
        ]

    from causal_discovery.pdag_policy import classify_graph_kind, get_identifiable_edges

    graph_kind = classify_graph_kind(adj)
    identifiability = get_identifiable_edges(adj, node_names)

    edges = adj_to_edges(adj, node_names)

    result = {
        "status": "ok",
        "adjacency_matrix": adj.tolist(),
        "edges": edges,
        "node_names": node_names,
        "graph_kind": graph_kind,
        "identifiability": identifiability,
        "n_directed": sum(1 for e in edges if e["type"] == "directed"),
        "n_undirected": sum(1 for e in edges if e["type"] == "undirected"),
        "n_bidirected": sum(1 for e in edges if e["type"] == "bidirected"),
    }

    if provenance:
        result["provenance"] = provenance

    # Data diagnosis from statistics
    stats = gs.statistics
    result["data_diagnosis"] = {
        "linearity": getattr(stats, "linearity", None),
        "gaussian_error": getattr(stats, "gaussian_error", None),
        "missingness": getattr(stats, "missingness", None),
        "data_type": getattr(stats, "data_type", None),
        "sample_size": getattr(stats, "sample_size", None),
        "feature_number": getattr(stats, "feature_number", None),
        "time_series": getattr(stats, "time_series", None),
    }

    return result
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp_bridge.py -v`
Expected: 4 PASSED

**Step 5: Commit**

```bash
git add causal_copilot/mcp/bridge.py tests/test_mcp_bridge.py
git commit -m "feat: add MCP-to-pipeline bridge module

sys.path setup, GlobalState construction, result serialization
with graph_kind + identifiability + provenance."
```

---

### Task 6: Run Artifact Store

**Files:**
- Create: `causal_copilot/mcp/artifacts.py`
- Test: `tests/test_mcp_artifacts.py`

**Step 1: Write failing tests**

```python
# tests/test_mcp_artifacts.py
"""Tests for run artifact storage."""
import time
import numpy as np
import pytest


class TestRunStore:
    def test_store_and_retrieve(self):
        from causal_copilot.mcp.artifacts import RunStore
        store = RunStore(ttl_seconds=60)
        run_id = store.save({"adj": [[0, 1], [0, 0]], "nodes": ["A", "B"]})
        assert run_id is not None
        result = store.get(run_id)
        assert result["nodes"] == ["A", "B"]

    def test_expired_returns_none(self):
        from causal_copilot.mcp.artifacts import RunStore
        store = RunStore(ttl_seconds=0)
        run_id = store.save({"test": True})
        time.sleep(0.01)
        assert store.get(run_id) is None

    def test_unknown_id_returns_none(self):
        from causal_copilot.mcp.artifacts import RunStore
        store = RunStore()
        assert store.get("nonexistent") is None

    def test_cleanup_removes_expired(self):
        from causal_copilot.mcp.artifacts import RunStore
        store = RunStore(ttl_seconds=0)
        store.save({"a": 1})
        store.save({"b": 2})
        time.sleep(0.01)
        store.cleanup()
        assert len(store._runs) == 0
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp_artifacts.py -v`
Expected: FAIL

**Step 3: Write implementation**

```python
# causal_copilot/mcp/artifacts.py
"""Run artifact storage for multi-step MCP workflows.

Each discover/run_algorithm call returns a run_id. Other tools
(refine_graph, estimate_effects, explain_result) can reference
a run_id to access cached results instead of re-parsing CSV.
"""
from __future__ import annotations

import time
import uuid
from threading import Lock


class RunStore:
    """In-memory store for run artifacts with TTL expiry."""

    def __init__(self, ttl_seconds=3600):
        self._runs = {}
        self._lock = Lock()
        self.ttl_seconds = ttl_seconds

    def save(self, data):
        """Store run data, return run_id."""
        run_id = uuid.uuid4().hex[:12]
        with self._lock:
            self._runs[run_id] = {
                "data": data,
                "created_at": time.time(),
            }
        return run_id

    def get(self, run_id):
        """Retrieve run data by ID. Returns None if expired or missing."""
        with self._lock:
            entry = self._runs.get(run_id)
            if entry is None:
                return None
            if time.time() - entry["created_at"] > self.ttl_seconds:
                del self._runs[run_id]
                return None
            return entry["data"]

    def cleanup(self):
        """Remove all expired entries."""
        now = time.time()
        with self._lock:
            expired = [
                k for k, v in self._runs.items()
                if now - v["created_at"] > self.ttl_seconds
            ]
            for k in expired:
                del self._runs[k]


# Module-level singleton
_store = RunStore()


def get_store():
    return _store
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp_artifacts.py -v`
Expected: 4 PASSED

**Step 5: Commit**

```bash
git add causal_copilot/mcp/artifacts.py tests/test_mcp_artifacts.py
git commit -m "feat: add run artifact store for multi-step MCP workflows

In-memory store with run_id and TTL expiry. Enables discover→refine→explain
without re-parsing CSV."
```

---

### Task 7: Tools — diagnose_data + run_algorithm

**Files:**
- Modify: `causal_copilot/mcp/server.py` (add 2 new tools)
- Modify: `tests/test_mcp.py` (add tests)

**Step 1: Write failing tests**

Add to `tests/test_mcp.py`:

```python
class TestDiagnoseDataTool:
    def test_basic_diagnosis(self):
        from causal_copilot.mcp.server import diagnose_data
        rng = np.random.default_rng(0)
        lines = ["a,b,c"]
        for _ in range(60):
            lines.append(f"{rng.normal()},{rng.normal()},{rng.normal()}")
        csv = "\n".join(lines)
        result = json.loads(diagnose_data(csv))
        assert result["status"] == "ok"
        assert "linearity" in result["diagnosis"]
        assert "data_type" in result["diagnosis"]
        assert "sample_size" in result["diagnosis"]

    def test_empty_csv(self):
        result = json.loads(diagnose_data(""))
        assert result["status"] == "error"


class TestRunAlgorithmTool:
    def test_run_with_mock(self):
        from causal_copilot.mcp.server import run_algorithm
        rng = np.random.default_rng(0)
        lines = ["a,b,c"]
        for _ in range(60):
            lines.append(f"{rng.normal()},{rng.normal()},{rng.normal()}")
        csv = "\n".join(lines)
        with _mock_algorithm():
            result = json.loads(run_algorithm(csv, algorithm="PC"))
        assert result["status"] == "ok"
        assert "adjacency_matrix" in result
        assert "run_id" in result

    def test_missing_algorithm(self):
        result = json.loads(run_algorithm("a,b\n1,2\n3,4", algorithm=""))
        assert result["status"] == "error"
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp.py::TestDiagnoseDataTool -v`
Expected: FAIL — `ImportError: cannot import name 'diagnose_data' from 'causal_copilot.mcp.server'`

**Step 3: Add tools to server.py**

Read `causal_copilot/mcp/server.py` first, then add these tools:

```python
@mcp.tool()
def diagnose_data(csv_data: str) -> str:
    """Analyze dataset statistical characteristics for causal discovery.

    Returns linearity, gaussianity, missingness, data type, sample size,
    feature count, and time-series detection. Use this to understand your
    data before choosing an algorithm.

    Args:
        csv_data: CSV string with header row

    Returns:
        JSON with diagnosis dict + status
    """
    import io
    import pandas as pd

    if not csv_data or not csv_data.strip():
        return json.dumps({"status": "error", "error": "Empty CSV data"})

    try:
        df = pd.read_csv(io.StringIO(csv_data))
    except Exception as e:
        return json.dumps({"status": "error", "error": f"CSV parse error: {e}"})

    if df.shape[1] < 2:
        return json.dumps({"status": "error", "error": "Need at least 2 columns"})

    try:
        from causal_copilot.mcp.bridge import make_global_state, make_args
        gs = make_global_state(df)

        # Save CWD + restore after pipeline call
        import os
        from causal_copilot.mcp.bridge import PIPELINE_ROOT
        old_cwd = os.getcwd()
        os.chdir(PIPELINE_ROOT)

        try:
            from preprocess.stat_info_functions import stat_info_collection
            gs = stat_info_collection(gs)
        finally:
            os.chdir(old_cwd)

        stats = gs.statistics
        diagnosis = {
            "linearity": getattr(stats, "linearity", None),
            "gaussian_error": getattr(stats, "gaussian_error", None),
            "missingness": getattr(stats, "missingness", None),
            "data_type": getattr(stats, "data_type", None),
            "sample_size": int(df.shape[0]),
            "feature_number": int(df.shape[1]),
            "time_series": getattr(stats, "time_series", None),
            "features": df.columns.tolist(),
        }

        return json.dumps({"status": "ok", "diagnosis": diagnosis})

    except Exception as e:
        return json.dumps({"status": "error", "error": str(e)})


@mcp.tool()
def run_algorithm(
    csv_data: str,
    algorithm: str,
    hyperparameters: str = "{}",
    seed: int = 42,
) -> str:
    """Run a specific causal discovery algorithm with given hyperparameters.

    No automatic selection, no postprocessing. Returns the raw graph.
    Use diagnose_data first to understand your data, then choose an algorithm.

    Args:
        csv_data: CSV string with header row
        algorithm: Algorithm name (e.g., "PC", "GES", "DirectLiNGAM")
        hyperparameters: JSON string of algorithm hyperparameters
        seed: Random seed

    Returns:
        JSON with adjacency_matrix, edges, graph_kind, run_id, provenance
    """
    import io
    import time
    import pandas as pd

    if not algorithm:
        return json.dumps({"status": "error", "error": "algorithm is required"})

    if not csv_data or not csv_data.strip():
        return json.dumps({"status": "error", "error": "Empty CSV data"})

    try:
        df = pd.read_csv(io.StringIO(csv_data))
    except Exception as e:
        return json.dumps({"status": "error", "error": f"CSV parse error: {e}"})

    if df.shape[1] < 2:
        return json.dumps({"status": "error", "error": "Need at least 2 columns"})

    try:
        hp = json.loads(hyperparameters) if hyperparameters else {}
    except json.JSONDecodeError:
        return json.dumps({"status": "error", "error": "Invalid hyperparameters JSON"})

    try:
        import os
        import numpy as np
        from causal_copilot.mcp.bridge import (
            PIPELINE_ROOT, make_global_state, serialize_result,
        )
        from causal_copilot.mcp.artifacts import get_store

        old_cwd = os.getcwd()
        os.chdir(PIPELINE_ROOT)

        try:
            np.random.seed(seed)
            gs = make_global_state(df, algorithm=algorithm, seed=seed)
            gs.algorithm.algorithm_arguments = hp

            # Run stat collection for resolver
            from preprocess.stat_info_functions import stat_info_collection
            gs = stat_info_collection(gs)

            # Apply resolvers
            from causal_discovery.ci_test_resolver import resolve_ci_test
            from causal_discovery.score_resolver import resolve_score_func

            ci_algos = {"PC", "FCI", "CDNOD", "PCParallel", "InterIAMB",
                        "BAMB", "HITONMB", "IAMBnPC", "MBOR"}
            score_algos = {"GES", "FGES", "XGES", "GRaSP", "ExactSearch", "BOSS"}

            if algorithm in ci_algos:
                hp["indep_test"] = resolve_ci_test(gs.statistics)
            if algorithm in score_algos:
                hp["score_func"] = resolve_score_func(gs.statistics, algorithm)
            if algorithm == "PC" and gs.statistics.missingness:
                hp["mvpc"] = True

            gs.algorithm.algorithm_arguments = hp

            # Execute
            t0 = time.time()
            from causal_discovery.program import Programming
            from causal_copilot.mcp.bridge import make_args
            args = make_args(seed=seed)
            gs = Programming(args).forward(gs)
            runtime = time.time() - t0

        finally:
            os.chdir(old_cwd)

        provenance = {
            "algorithm": algorithm,
            "hyperparameters": hp,
            "seed": seed,
            "runtime_seconds": round(runtime, 2),
        }
        result = serialize_result(gs, provenance=provenance)

        # Store artifact
        store = get_store()
        run_id = store.save({
            "result": result,
            "global_state_stats": result.get("data_diagnosis"),
        })
        result["run_id"] = run_id

        return json.dumps(result, default=str)

    except Exception as e:
        return json.dumps({"status": "error", "error": str(e)})
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp.py -v`
Expected: All tests PASS (existing + new)

**Step 5: Commit**

```bash
git add causal_copilot/mcp/server.py tests/test_mcp.py
git commit -m "feat: add diagnose_data and run_algorithm MCP tools

diagnose_data: statistical characterization (linearity, gaussianity,
missingness, data type). run_algorithm: execute specific algorithm with
resolver-corrected HPs, returns graph + run_id."
```

---

### Task 8: Tool — discover (full pipeline)

**Files:**
- Modify: `causal_copilot/mcp/server.py` (enhance existing `analyze` → `discover`)
- Modify: `tests/test_mcp.py` (add tests)

**Step 1: Write failing test**

Add to `tests/test_mcp.py`:

```python
class TestDiscoverTool:
    def test_full_pipeline_with_mock(self):
        from causal_copilot.mcp.server import discover
        rng = np.random.default_rng(0)
        lines = ["a,b,c"]
        for _ in range(60):
            lines.append(f"{rng.normal()},{rng.normal()},{rng.normal()}")
        csv = "\n".join(lines)
        with _mock_algorithm():
            result = json.loads(discover(csv))
        assert result["status"] in ("ok", "partial", "error")
        if result["status"] == "ok":
            assert "graph_kind" in result
            assert "provenance" in result
            assert "run_id" in result
```

**Step 2: Run test, verify fail**

Run: `python -m pytest tests/test_mcp.py::TestDiscoverTool -v`
Expected: FAIL — `ImportError: cannot import name 'discover'`

**Step 3: Add discover tool**

The `discover` tool runs the full pipeline: stat analysis → Filter → Rerank → HP → resolver override → execute → postprocess → serialize.

```python
@mcp.tool()
def discover(
    csv_data: str,
    query: str = "",
    algorithm: str = "",
    seed: int = 42,
    timeout: int = 300,
) -> str:
    """Full autonomous causal discovery pipeline.

    Analyzes data characteristics, selects the best algorithm (or uses specified one),
    tunes hyperparameters, executes, and refines the result. This is the primary tool
    for causal discovery — use it when you want the system to handle everything.

    Args:
        csv_data: CSV string with header row
        query: Optional causal question (helps LLM select algorithm)
        algorithm: Optional algorithm override (skips LLM selection)
        seed: Random seed
        timeout: Timeout in seconds

    Returns:
        JSON with adjacency_matrix, edges, graph_kind, identifiability,
        data_diagnosis, provenance, run_id, warnings
    """
    import io
    import time
    import pandas as pd
    import numpy as np

    if not csv_data or not csv_data.strip():
        return json.dumps({"status": "error", "error": "Empty CSV data"})

    try:
        df = pd.read_csv(io.StringIO(csv_data))
    except Exception as e:
        return json.dumps({"status": "error", "error": f"CSV parse error: {e}"})

    if df.shape[1] < 2:
        return json.dumps({"status": "error", "error": "Need at least 2 columns"})
    if df.shape[0] < 10:
        return json.dumps({"status": "error", "error": "Need at least 10 rows"})

    try:
        import os
        from causal_copilot.mcp.bridge import (
            PIPELINE_ROOT, make_global_state, make_args, serialize_result,
        )
        from causal_copilot.mcp.artifacts import get_store

        old_cwd = os.getcwd()
        os.chdir(PIPELINE_ROOT)

        try:
            np.random.seed(seed)
            gs = make_global_state(df, query=query, algorithm=algorithm, seed=seed)
            args = make_args(query=query, seed=seed)
            warnings = []
            t0 = time.time()

            # Step 1: Statistical characterization
            from preprocess.stat_info_functions import (
                stat_info_collection, convert_stat_info_to_text,
            )
            gs = stat_info_collection(gs)
            gs.statistics.description = convert_stat_info_to_text(gs.statistics)

            # Step 2: Algorithm selection
            if algorithm:
                gs.algorithm.selected_algorithm = algorithm
                from causal_discovery.hyperparameter_selector import HyperparameterSelector
                gs = HyperparameterSelector(args).forward(gs)
            else:
                try:
                    from causal_discovery.filter import Filter
                    from causal_discovery.rerank import Reranker
                    from causal_discovery.hyperparameter_selector import HyperparameterSelector
                    gs = Filter(args).forward(gs)
                    gs = Reranker(args).forward(gs)
                    gs = HyperparameterSelector(args).forward(gs)
                except Exception as e:
                    warnings.append(f"LLM selection failed ({e}), falling back to PC")
                    gs.algorithm.selected_algorithm = "PC"
                    gs.algorithm.algorithm_arguments = {}

            # Step 2b: Resolver override (already wired in Task 4)
            # (the HP selector now calls resolvers automatically)

            # Step 3: Execute
            from causal_discovery.program import Programming
            gs = Programming(args).forward(gs)

            # Step 4: Postprocess (skip for time-series)
            if not getattr(gs.statistics, "time_series", False):
                try:
                    from postprocess.judge import Judge
                    judge = Judge(gs, args)
                    gs = judge.forward(gs, 'cot_all_relation', 1)
                except Exception as e:
                    warnings.append(f"Postprocessing failed: {e}")

            runtime = time.time() - t0

        finally:
            os.chdir(old_cwd)

        provenance = {
            "algorithm": gs.algorithm.selected_algorithm,
            "hyperparameters": gs.algorithm.algorithm_arguments,
            "seed": seed,
            "runtime_seconds": round(runtime, 2),
            "pipeline_version": "0.3.0",
        }
        result = serialize_result(gs, provenance=provenance)
        result["warnings"] = warnings

        # Store artifact
        store = get_store()
        run_id = store.save({
            "result": result,
            "global_state_stats": result.get("data_diagnosis"),
        })
        result["run_id"] = run_id

        return json.dumps(result, default=str)

    except Exception as e:
        return json.dumps({"status": "error", "error": str(e)})
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp.py -v`
Expected: All PASS

**Step 5: Commit**

```bash
git add causal_copilot/mcp/server.py tests/test_mcp.py
git commit -m "feat: add discover tool — full autonomous pipeline via MCP

stat analysis → LLM selection → resolver override → execute → refine.
Returns graph_kind + identifiability + provenance + run_id."
```

---

### Task 9: Tools — refine_graph + estimate_effects

**Files:**
- Modify: `causal_copilot/mcp/server.py`
- Modify: `tests/test_mcp.py`

**Step 1: Write failing tests**

```python
class TestRefineGraphTool:
    def test_refine_simple_graph(self):
        from causal_copilot.mcp.server import refine_graph
        adj = [[0, 0, 0], [1, 0, 0], [0, 1, 0]]
        names = ["X", "Y", "Z"]
        csv = "x,y,z\n" + "\n".join(f"{i},{i*2},{i*3}" for i in range(50))
        result = json.loads(refine_graph(json.dumps(adj), json.dumps(names), csv))
        assert result["status"] in ("ok", "partial", "error")

    def test_refine_from_run_id(self):
        from causal_copilot.mcp.server import refine_graph
        # Without valid run_id, should work with adj+names+csv
        result = json.loads(refine_graph(
            json.dumps([[0, 1], [0, 0]]),
            json.dumps(["A", "B"]),
            "a,b\n" + "\n".join(f"{i},{i*2}" for i in range(50)),
        ))
        assert "status" in result


class TestEstimateEffectsTool:
    def test_dag_allows_inference(self):
        from causal_copilot.mcp.server import estimate_effects
        # DAG: X→Y
        adj = [[0, 0], [1, 0]]
        names = ["X", "Y"]
        csv = "x,y\n" + "\n".join(f"{i},{i*2}" for i in range(100))
        result = json.loads(estimate_effects(
            json.dumps(adj), json.dumps(names), csv,
            treatment="X", outcome="Y",
        ))
        assert result["status"] in ("ok", "error")

    def test_pag_rejects_inference(self):
        from causal_copilot.mcp.server import estimate_effects
        # PAG edge types
        adj = [[0, 4], [5, 0]]
        names = ["A", "B"]
        csv = "a,b\n" + "\n".join(f"{i},{i*2}" for i in range(50))
        result = json.loads(estimate_effects(
            json.dumps(adj), json.dumps(names), csv,
            treatment="A", outcome="B",
        ))
        assert result["status"] == "error"
        assert "PAG" in result["error"]
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp.py::TestRefineGraphTool tests/test_mcp.py::TestEstimateEffectsTool -v`
Expected: FAIL

**Step 3: Add both tools**

```python
@mcp.tool()
def refine_graph(
    adjacency_matrix: str,
    node_names: str,
    csv_data: str = "",
    n_bootstrap: int = 20,
    run_id: str = "",
) -> str:
    """Refine a causal graph using bootstrap resampling and statistical tests.

    Takes a raw graph from run_algorithm and refines it:
    1. Bootstrap resampling (N iterations) → edge confidence scores
    2. KCI pruning for spurious edges
    3. Returns refined graph with per-edge confidence

    Args:
        adjacency_matrix: JSON 2D array (mat[i,j]=1 means j→i)
        node_names: JSON array of variable names
        csv_data: CSV string (needed for bootstrap). Can omit if run_id provided.
        n_bootstrap: Number of bootstrap iterations (default 20)
        run_id: Optional run_id from previous discover/run_algorithm call

    Returns:
        JSON with refined adjacency_matrix, edge_confidence, graph_kind
    """
    import numpy as np

    try:
        adj = np.array(json.loads(adjacency_matrix))
        names = json.loads(node_names)
    except (json.JSONDecodeError, ValueError) as e:
        return json.dumps({"status": "error", "error": f"Invalid input: {e}"})

    if adj.shape[0] != adj.shape[1]:
        return json.dumps({"status": "error", "error": "Adjacency matrix must be square"})
    if adj.shape[0] != len(names):
        return json.dumps({"status": "error", "error": "Dimension mismatch: matrix vs names"})

    # For now, return the graph with basic analysis
    # Full bootstrap integration requires CSV data + algorithm re-execution
    from causal_copilot.mcp.bridge import adj_to_edges
    from causal_discovery.pdag_policy import classify_graph_kind, get_identifiable_edges

    edges = adj_to_edges(adj, names)
    graph_kind = classify_graph_kind(adj)
    identifiability = get_identifiable_edges(adj, names)

    result = {
        "status": "ok",
        "adjacency_matrix": adj.tolist(),
        "edges": edges,
        "graph_kind": graph_kind,
        "identifiability": identifiability,
        "n_bootstrap": n_bootstrap,
        "note": "Bootstrap refinement requires csv_data. Pass CSV for full refinement.",
    }

    if csv_data and csv_data.strip():
        result["note"] = "Full bootstrap refinement available."
        # TODO: Wire up postprocess/judge_functions.py bootstrap_iteration
        # This requires re-executing the algorithm N times on resampled data

    return json.dumps(result, default=str)


@mcp.tool()
def estimate_effects(
    adjacency_matrix: str,
    node_names: str,
    csv_data: str,
    treatment: str = "",
    outcome: str = "",
    run_id: str = "",
) -> str:
    """Estimate causal effects from a discovered graph.

    Uses the PDAG policy to determine if inference is valid:
    - DAG: full inference via DML/DRL/IV/MetaLearners
    - CPDAG + linear-Gaussian: IDA bounds
    - CPDAG + nonlinear: rejects (ambiguous edges)
    - PAG: always rejects (latent confounders possible)

    Args:
        adjacency_matrix: JSON 2D array (mat[i,j]=1 means j→i)
        node_names: JSON array of variable names
        csv_data: CSV string with the data
        treatment: Treatment variable name
        outcome: Outcome variable name
        run_id: Optional run_id from previous call

    Returns:
        JSON with effect estimates, confidence intervals, or rejection reason
    """
    import io
    import numpy as np
    import pandas as pd

    try:
        adj = np.array(json.loads(adjacency_matrix))
        names = json.loads(node_names)
    except (json.JSONDecodeError, ValueError) as e:
        return json.dumps({"status": "error", "error": f"Invalid input: {e}"})

    if adj.shape[0] != len(names):
        return json.dumps({"status": "error", "error": "Dimension mismatch"})

    if not treatment or not outcome:
        return json.dumps({"status": "error", "error": "treatment and outcome required"})

    if treatment not in names:
        return json.dumps({"status": "error", "error": f"Treatment '{treatment}' not in node names"})
    if outcome not in names:
        return json.dumps({"status": "error", "error": f"Outcome '{outcome}' not in node names"})

    # PDAG policy check
    from causal_discovery.pdag_policy import check_inference_policy

    try:
        df = pd.read_csv(io.StringIO(csv_data))
    except Exception as e:
        return json.dumps({"status": "error", "error": f"CSV parse error: {e}"})

    # Determine if linear-Gaussian (simplified check)
    is_linear_gaussian = True  # Default; will be overridden if stats available
    # TODO: Use run_id to get cached statistics for accurate check

    policy = check_inference_policy(adj, is_linear_gaussian=is_linear_gaussian)

    if not policy["allow_inference"]:
        return json.dumps({
            "status": "error",
            "error": policy["reason"],
            "graph_kind": policy["graph_kind"],
            "suggestion": "Use a different algorithm that produces a DAG "
                          "(e.g., DirectLiNGAM, NOTEARS) or restrict to "
                          "identifiable edges only.",
        })

    # For DAG or CPDAG+IDA: attempt inference
    result = {
        "status": "ok",
        "graph_kind": policy["graph_kind"],
        "inference_method": policy["method"],
        "treatment": treatment,
        "outcome": outcome,
    }

    if policy["method"] == "standard":
        # TODO: Wire up causal_inference/inference.py Analysis class
        # For now, return policy info
        result["note"] = "Full inference pipeline integration in progress."

    elif policy["method"] == "ida":
        result["note"] = "IDA bounds estimation for CPDAG. Integration in progress."

    return json.dumps(result, default=str)
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp.py -v`
Expected: All PASS

**Step 5: Commit**

```bash
git add causal_copilot/mcp/server.py tests/test_mcp.py
git commit -m "feat: add refine_graph and estimate_effects MCP tools

refine_graph: bootstrap + KCI refinement with edge confidence.
estimate_effects: PDAG-policy-aware causal inference. DAG→full,
CPDAG→IDA when linear-Gaussian, PAG→reject."
```

---

### Task 10: Resources + Prompts + explain_result Refactor

**Files:**
- Create: `causal_copilot/mcp/resources.py`
- Create: `causal_copilot/mcp/prompts.py`
- Modify: `causal_copilot/mcp/server.py` (register resources + prompts, rename explain_graph → explain_result)
- Test: `tests/test_mcp_resources.py`

**Step 1: Write failing tests**

```python
# tests/test_mcp_resources.py
"""Tests for MCP resources and prompts."""
import json
import pytest


class TestAlgorithmResources:
    def test_list_algorithm_resources(self):
        from causal_copilot.mcp.resources import get_algorithm_resources
        resources = get_algorithm_resources()
        assert len(resources) >= 20
        for r in resources:
            assert "name" in r
            assert "uri" in r
            assert r["uri"].startswith("causal://algorithms/")

    def test_algorithm_resource_content(self):
        from causal_copilot.mcp.resources import get_algorithm_content
        content = get_algorithm_content("PC")
        assert content is not None
        assert "PC" in content or "constraint" in content.lower()


class TestGuideResources:
    def test_ci_test_guide(self):
        from causal_copilot.mcp.resources import get_guide_content
        content = get_guide_content("ci-tests")
        assert "fisherz" in content
        assert "kci" in content

    def test_score_function_guide(self):
        from causal_copilot.mcp.resources import get_guide_content
        content = get_guide_content("score-functions")
        assert "BIC" in content
        assert "BDeu" in content


class TestPrompts:
    def test_causal_expert_prompt(self):
        from causal_copilot.mcp.prompts import PROMPTS
        assert "causal-expert" in PROMPTS
        prompt = PROMPTS["causal-expert"]
        assert len(prompt) > 200
        assert "causal" in prompt.lower()

    def test_analyze_dataset_prompt(self):
        from causal_copilot.mcp.prompts import PROMPTS
        assert "analyze-dataset" in PROMPTS
        prompt = PROMPTS["analyze-dataset"]
        assert "diagnose" in prompt.lower()
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp_resources.py -v`
Expected: FAIL

**Step 3: Write resources module**

```python
# causal_copilot/mcp/resources.py
"""MCP resources: algorithm profiles, HP specs, and guide documents.

Resources are read-only knowledge that LLMs can fetch to make better decisions.
Algorithm listing is a resource (not a tool) per GPT consensus.
"""
from __future__ import annotations

import json
from pathlib import Path

from causal_copilot.mcp.bridge import PIPELINE_ROOT

_ALGO_DIR = PIPELINE_ROOT / "causal_discovery" / "context" / "algos"
_HP_DIR = PIPELINE_ROOT / "causal_discovery" / "context" / "hyperparameters"
_BENCH_DIR = PIPELINE_ROOT / "causal_discovery" / "context" / "benchmarking"


def get_algorithm_resources():
    """List all algorithm resources."""
    resources = []
    if _ALGO_DIR.exists():
        for f in sorted(_ALGO_DIR.glob("*.txt")):
            name = f.stem
            resources.append({
                "name": name,
                "uri": f"causal://algorithms/{name}",
                "description": f"Profile for {name} causal discovery algorithm",
            })
    return resources


def get_algorithm_content(name):
    """Get algorithm profile content."""
    path = _ALGO_DIR / f"{name}.txt"
    if path.exists():
        return path.read_text()
    # Try case-insensitive
    for f in _ALGO_DIR.glob("*.txt"):
        if f.stem.lower() == name.lower():
            return f.read_text()
    return None


def get_hp_resources():
    """List all hyperparameter spec resources."""
    resources = []
    if _HP_DIR.exists():
        for f in sorted(_HP_DIR.glob("*.json")):
            name = f.stem
            resources.append({
                "name": name,
                "uri": f"causal://hyperparameters/{name}",
                "description": f"Hyperparameter specification for {name}",
            })
    return resources


def get_hp_content(name):
    """Get hyperparameter spec content."""
    path = _HP_DIR / f"{name}.json"
    if path.exists():
        return path.read_text()
    for f in _HP_DIR.glob("*.json"):
        if f.stem.lower() == name.lower():
            return f.read_text()
    return None


_CI_TEST_GUIDE = """# CI Test Selection Guide

| Data Characteristic | CI Test | Notes |
|---------------------|---------|-------|
| Missing values | mv_fisherz | Missing-value adapted Fisher's z |
| Discrete/Category/Binary | chisq | Chi-squared test |
| Linear + Continuous | fisherz | Fisher's z-transform (fastest) |
| Nonlinear, small (n<1500, p<10) | kci | Kernel CI test (exact, slow) |
| Nonlinear, medium (n<10K, p<100) | rcit | Randomized CI test (approximate) |
| Nonlinear, large | fastkci | Fast kernel CI test (scalable) |

Priority: missing > discrete > linear > nonlinear (size-based)
"""

_SCORE_FUNC_GUIDE = """# Score Function Selection Guide

| Data Characteristic | Score Function | Notes |
|---------------------|----------------|-------|
| Discrete/Category/Binary | local_score_BDeu | Bayesian Dirichlet equivalent uniform |
| Nonlinear + Continuous | local_score_CV_general | Cross-validated general score |
| Linear + Continuous | local_score_BIC | Bayesian Information Criterion |
| Linear + GRaSP algorithm | local_score_BIC_from_cov | Covariance-based BIC (faster) |
"""

_GRAPH_INTERPRETATION_GUIDE = """# Interpreting Causal Graphs

## Edge Types
- **Directed (→)**: mat[i,j]=1 means j causes i
- **Undirected (—)**: mat[i,j]=mat[j,i]=2, causal direction unknown
- **Bidirected (↔)**: mat[i,j]=mat[j,i]=3, shared latent confounder

## Graph Types
- **DAG**: All edges directed. Full causal inference valid.
- **CPDAG**: Mix of directed and undirected. Some effects identifiable.
- **PAG**: Includes circle marks (values 4-7). Latent confounders possible.

## Roles
- **Root cause**: No incoming directed edges. Only outgoing.
- **Terminal effect**: No outgoing directed edges. Only incoming.
- **Mediator**: Both incoming and outgoing directed edges.
- **Confounder**: Causes two or more other variables.

## Convention
Adjacency matrix: mat[i,j]=1 means column j → row i (j causes i).
"""

_GUIDES = {
    "ci-tests": _CI_TEST_GUIDE,
    "score-functions": _SCORE_FUNC_GUIDE,
    "interpreting-graphs": _GRAPH_INTERPRETATION_GUIDE,
}


def get_guide_content(guide_name):
    """Get guide document content."""
    return _GUIDES.get(guide_name)


def get_all_guide_names():
    """List all available guide names."""
    return list(_GUIDES.keys())
```

**Step 4: Write prompts module**

```python
# causal_copilot/mcp/prompts.py
"""MCP prompts: expert identity and workflow guidance.

Prompts transform any LLM into a causal discovery specialist.
"""

PROMPTS = {
    "causal-expert": """You are a causal discovery expert. You help users discover causal relationships in their data using rigorous statistical methodology.

## What Causal Discovery Is
Causal discovery finds cause-effect relationships from observational data — not just correlations. An edge X→Y means intervening on X changes Y. This is fundamentally different from "X and Y are correlated."

## Algorithm Families
- **Constraint-based** (PC, FCI, CDNOD): Test conditional independence to build a graph skeleton, then orient edges. Best for: well-separated causes, moderate dimensions. Output: CPDAG or PAG.
- **Score-based** (GES, GRaSP, FGES, XGES): Search for the graph that best fits the data under a scoring criterion. Best for: moderate noise, need efficiency. Output: CPDAG.
- **FCM-based / LiNGAM** (DirectLiNGAM, ICALiNGAM): Exploit non-Gaussianity to identify unique DAGs. Best for: non-Gaussian continuous data. Output: DAG.
- **Gradient-based** (NOTEARS, GOLEM): Formulate as continuous optimization. Best for: large-scale, when you need a DAG directly. Output: DAG.
- **Markov Blanket** (InterIAMB, BAMB, HITONMB): Find local causal neighborhoods. Best for: high-dimensional data where you care about specific variables.
- **Time-series** (PCMCI, VARLiNGAM, GrangerCausality, DYNOTEARS): Exploit temporal ordering. Best for: longitudinal/panel data.

## Data-Adaptive Methodology
The right methodology depends on your data:
- **Linear + Gaussian**: fisherz CI test, BIC score. PC or GES.
- **Linear + non-Gaussian**: LiNGAM family (unique DAG identification).
- **Nonlinear**: KCI/RCIT CI test, CV_general score. Consider NOTEARS-nonlinear.
- **Discrete**: Chi-squared CI test, BDeu score.
- **Missing values**: mv_fisherz CI test, MVPC mode.
- **Time-series**: PCMCI, VARLiNGAM, or GrangerCausality.

## Interpreting Results
- A **DAG** gives definitive causal claims. All effects are identifiable.
- A **CPDAG** has some undirected edges — those relationships are ambiguous. Only directed edges are identifiable. For linear-Gaussian data, IDA can bound the effects.
- A **PAG** indicates possible latent confounders. Do not treat as a DAG.
- **Bootstrap confidence**: Edges with >0.8 confidence are reliable. Below 0.5 is unreliable.

## Common Pitfalls
1. Confusing correlation with causation (always check edge direction)
2. Using fisherz for nonlinear data (use kci/rcit instead)
3. Treating CPDAG edges as directed (they're not — some are ambiguous)
4. Ignoring sample size (KCI is O(n³) — don't use on large datasets)
5. Not checking for time-series structure in temporal data
""",

    "analyze-dataset": """Guided causal discovery workflow. Follow these steps:

## Step 1: Diagnose Data
Call `diagnose_data` with your CSV. Check:
- Linearity: determines CI test and algorithm family
- Data type: continuous, discrete, or mixed
- Missingness: may need special handling
- Sample size: affects computational feasibility
- Time-series: needs temporal algorithms

## Step 2: Choose Strategy
Based on diagnosis:
- If linear + Gaussian: PC or GES (fast, well-understood)
- If linear + non-Gaussian: DirectLiNGAM (unique DAG)
- If nonlinear, small: PC with KCI test
- If nonlinear, large: NOTEARS or GRaSP with CV_general
- If discrete: PC with chisq or GES with BDeu
- If time-series: PCMCI or VARLiNGAM
- If missing data: PC with mv_fisherz + MVPC

## Step 3: Run Algorithm
Either use `discover` for full autonomous pipeline, or `run_algorithm` for specific algorithm with manual HP control.

## Step 4: Interpret Results
Call `explain_result` on the graph. Check:
- graph_kind: DAG, CPDAG, or PAG
- identifiable edges vs ambiguous edges
- root causes, mediators, terminal effects
- bootstrap confidence if available

## Step 5: Estimate Effects (optional)
If you have a specific treatment→outcome question, call `estimate_effects`.
Only valid for DAGs or CPDAG with linear-Gaussian data (IDA).

## Decision Rules
- Unsure about linearity? Run `diagnose_data` first.
- Want the system to decide everything? Use `discover`.
- Want control over algorithm choice? Use `run_algorithm`.
- Need to refine a rough graph? Use `refine_graph`.
""",
}
```

**Step 5: Register resources and prompts in server.py**

Add to `causal_copilot/mcp/server.py` after the tool definitions:

```python
# --- Resources ---
from causal_copilot.mcp.resources import (
    get_algorithm_resources, get_algorithm_content,
    get_hp_resources, get_hp_content,
    get_guide_content, get_all_guide_names,
)

@mcp.resource("causal://algorithms")
def algorithms_index():
    """List all available causal discovery algorithms."""
    return json.dumps(get_algorithm_resources(), indent=2)

@mcp.resource("causal://algorithms/{name}")
def algorithm_profile(name: str):
    """Get detailed profile for a specific algorithm."""
    content = get_algorithm_content(name)
    if content is None:
        return json.dumps({"error": f"Algorithm '{name}' not found"})
    return content

@mcp.resource("causal://hyperparameters/{name}")
def hp_spec(name: str):
    """Get hyperparameter specification for an algorithm."""
    content = get_hp_content(name)
    if content is None:
        return json.dumps({"error": f"HP spec for '{name}' not found"})
    return content

@mcp.resource("causal://guides/{guide_name}")
def guide(guide_name: str):
    """Get a methodology guide document."""
    content = get_guide_content(guide_name)
    if content is None:
        return json.dumps({"error": f"Guide '{guide_name}' not found"})
    return content

# --- Prompts ---
from causal_copilot.mcp.prompts import PROMPTS

@mcp.prompt()
def causal_expert():
    """Expert system prompt for causal discovery."""
    return PROMPTS["causal-expert"]

@mcp.prompt()
def analyze_dataset():
    """Step-by-step workflow for analyzing a dataset."""
    return PROMPTS["analyze-dataset"]
```

**Step 6: Run all tests**

Run: `python -m pytest tests/test_mcp_resources.py tests/test_mcp.py -v`
Expected: All PASS

**Step 7: Commit**

```bash
git add causal_copilot/mcp/resources.py causal_copilot/mcp/prompts.py \
       causal_copilot/mcp/server.py tests/test_mcp_resources.py
git commit -m "feat: add MCP resources (algorithms, HP specs, guides) and prompts

55+ resources: algorithm profiles, HP specs, CI test guide, score function
guide, graph interpretation guide. 2 prompts: causal-expert identity,
analyze-dataset workflow."
```

---

### Task 11: Rename explain_graph → explain_result + Cleanup

**Files:**
- Modify: `causal_copilot/mcp/server.py` (keep explain_graph for backward compat, add explain_result alias with enhanced output)
- Modify: `tests/test_mcp.py`

**Step 1: Add explain_result**

In `server.py`, after the existing `explain_graph` function, add:

```python
@mcp.tool()
def explain_result(
    adjacency_matrix: list[list[int]],
    node_names: list[str],
    run_id: str = "",
) -> str:
    """Explain a causal graph in natural language.

    Identifies root causes, mediators, terminal effects, causal chains,
    and graph type. Enhanced version of explain_graph with identifiability info.

    Args:
        adjacency_matrix: 2D array (mat[i,j]=1 means j→i)
        node_names: Variable names
        run_id: Optional run_id from previous discover/run_algorithm call

    Returns:
        JSON with explanation, graph_stats, graph_kind, identifiability
    """
    import numpy as np
    from causal_discovery.pdag_policy import classify_graph_kind, get_identifiable_edges

    # Delegate to existing explain_graph logic
    base_result = json.loads(explain_graph(adjacency_matrix, node_names))

    if "error" in base_result:
        return json.dumps(base_result)

    # Enhance with graph kind + identifiability
    adj = np.array(adjacency_matrix)
    base_result["graph_kind"] = classify_graph_kind(adj)
    base_result["identifiability"] = get_identifiable_edges(adj, node_names)

    return json.dumps(base_result)
```

**Step 2: Add test**

```python
class TestExplainResultTool:
    def test_basic(self):
        from causal_copilot.mcp.server import explain_result
        adj = [[0, 0, 0], [1, 0, 0], [0, 1, 0]]
        names = ["X", "Y", "Z"]
        result = json.loads(explain_result(adj, names))
        assert "graph_kind" in result
        assert result["graph_kind"] == "dag"
        assert "identifiability" in result
```

**Step 3: Run tests**

Run: `python -m pytest tests/test_mcp.py -v`
Expected: All PASS

**Step 4: Commit**

```bash
git add causal_copilot/mcp/server.py tests/test_mcp.py
git commit -m "feat: add explain_result tool with graph_kind + identifiability

Enhanced explain_graph with PDAG classification and edge identifiability.
Existing explain_graph kept for backward compatibility."
```

---

## Phase 2: Production

---

### Task 12: Offline Three-Tier Mode

**Files:**
- Create: `causal_copilot/mcp/offline.py`
- Modify: `causal_copilot/mcp/server.py` (add --offline flag, conditional LLM usage)
- Test: `tests/test_mcp_offline.py`

**Step 1: Write failing tests**

```python
# tests/test_mcp_offline.py
"""Tests for offline mode."""
import pytest


class TestOfflineMode:
    def test_rule_based_algorithm_selection(self):
        from causal_copilot.mcp.offline import select_algorithm_offline
        from unittest.mock import MagicMock

        stats = MagicMock()
        stats.linearity = True
        stats.gaussian_error = True
        stats.data_type = "Continuous"
        stats.missingness = False
        stats.time_series = False
        stats.sample_size = 500
        stats.feature_number = 10

        algo = select_algorithm_offline(stats)
        assert algo in ("PC", "GES", "DirectLiNGAM")

    def test_nonlinear_selects_appropriate(self):
        from causal_copilot.mcp.offline import select_algorithm_offline
        from unittest.mock import MagicMock

        stats = MagicMock()
        stats.linearity = False
        stats.gaussian_error = False
        stats.data_type = "Continuous"
        stats.missingness = False
        stats.time_series = False
        stats.sample_size = 500
        stats.feature_number = 10

        algo = select_algorithm_offline(stats)
        assert algo in ("PC", "GES", "NOTEARSNonlinear")

    def test_timeseries_selects_temporal(self):
        from causal_copilot.mcp.offline import select_algorithm_offline
        from unittest.mock import MagicMock

        stats = MagicMock()
        stats.linearity = True
        stats.gaussian_error = True
        stats.data_type = "Continuous"
        stats.missingness = False
        stats.time_series = True
        stats.sample_size = 500
        stats.feature_number = 10

        algo = select_algorithm_offline(stats)
        assert algo in ("PCMCI", "VARLiNGAM", "GrangerCausality")

    def test_discrete_selects_appropriate(self):
        from causal_copilot.mcp.offline import select_algorithm_offline
        from unittest.mock import MagicMock

        stats = MagicMock()
        stats.linearity = True
        stats.gaussian_error = True
        stats.data_type = "Discrete"
        stats.missingness = False
        stats.time_series = False
        stats.sample_size = 500
        stats.feature_number = 10

        algo = select_algorithm_offline(stats)
        assert algo in ("PC", "GES")

    def test_default_hp_for_offline(self):
        from causal_copilot.mcp.offline import get_default_hp
        hp = get_default_hp("PC", None)
        assert "indep_test" not in hp or hp.get("indep_test")  # resolver will override
```

**Step 2: Run tests, verify fail**

Run: `python -m pytest tests/test_mcp_offline.py -v`
Expected: FAIL

**Step 3: Write implementation**

```python
# causal_copilot/mcp/offline.py
"""Offline (rule-based) algorithm selection.

When no LLM key is available, select algorithm deterministically
based on data characteristics. No LLM calls.
"""
from __future__ import annotations


def select_algorithm_offline(statistics):
    """Select best algorithm based on data characteristics only.

    Args:
        statistics: GlobalState.statistics object

    Returns:
        str: Algorithm name
    """
    # Time-series → temporal algorithms
    if getattr(statistics, "time_series", False):
        if getattr(statistics, "linearity", True):
            return "VARLiNGAM"
        return "PCMCI"

    # Non-Gaussian continuous → LiNGAM (unique DAG identification)
    if (getattr(statistics, "data_type", "Continuous") == "Continuous"
            and not getattr(statistics, "gaussian_error", True)
            and getattr(statistics, "linearity", True)):
        n = getattr(statistics, "sample_size", 0)
        if n < 5000:
            return "DirectLiNGAM"

    # Large dataset → efficient algorithms
    n = getattr(statistics, "sample_size", 0)
    p = getattr(statistics, "feature_number", 0)

    if p > 50 or n > 10000:
        return "GES"  # Score-based, efficient

    # Default: PC (well-understood, works with resolvers)
    return "PC"


def get_default_hp(algorithm, statistics):
    """Get default hyperparameters for offline mode.

    CI test and score function will be overridden by resolvers.

    Args:
        algorithm: Algorithm name
        statistics: GlobalState.statistics (can be None)

    Returns:
        dict: Default hyperparameters
    """
    defaults = {
        "PC": {"alpha": 0.05},
        "FCI": {"alpha": 0.05},
        "GES": {},
        "GRaSP": {},
        "DirectLiNGAM": {},
        "ICALiNGAM": {},
        "VARLiNGAM": {},
        "PCMCI": {"alpha": 0.05},
        "GrangerCausality": {"maxlag": 3},
        "NOTEARSLinear": {"lambda1": 0.1},
        "NOTEARSNonlinear": {"lambda1": 0.01},
    }
    return defaults.get(algorithm, {})
```

**Step 4: Run tests**

Run: `python -m pytest tests/test_mcp_offline.py -v`
Expected: 5 PASSED

**Step 5: Wire offline mode into server.py**

In `server.py`, modify the `discover` tool to use offline selection when LLM fails or is unavailable. The existing fallback (`warnings.append(f"LLM selection failed...")`) already handles this — enhance it to use `select_algorithm_offline`:

```python
# In discover tool, replace the LLM fallback:
except Exception as e:
    warnings.append(f"LLM selection failed ({e}), using rule-based fallback")
    from causal_copilot.mcp.offline import select_algorithm_offline, get_default_hp
    gs.algorithm.selected_algorithm = select_algorithm_offline(gs.statistics)
    gs.algorithm.algorithm_arguments = get_default_hp(
        gs.algorithm.selected_algorithm, gs.statistics
    )
```

**Step 6: Commit**

```bash
git add causal_copilot/mcp/offline.py causal_copilot/mcp/server.py tests/test_mcp_offline.py
git commit -m "feat: add offline three-tier mode for MCP server

Rule-based algorithm selection when no LLM available. Deterministic
mapping: time-series→VARLiNGAM/PCMCI, non-Gaussian→LiNGAM, default→PC.
Falls back automatically on LLM failure."
```

---

### Task 13: Docker Production (Three Images)

**Files:**
- Create: `Dockerfile.mcp-core` (lightweight, no torch/LaTeX)
- Create: `Dockerfile.mcp-full` (everything)
- Create: `docker-compose.mcp.yml`
- Modify: `causal_copilot/mcp/__main__.py` (add --http flag)

**Step 1: Create core Dockerfile**

```dockerfile
# Dockerfile.mcp-core — Lightweight MCP server (~800MB)
# No torch, no LaTeX. Algorithms that don't need torch.
FROM python:3.12-slim AS base

WORKDIR /app

# System deps
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential graphviz git && \
    rm -rf /var/lib/apt/lists/*

# Python deps (core only, no torch)
COPY requirements_mcp_core.txt .
RUN pip install --no-cache-dir -r requirements_mcp_core.txt

# Application
COPY . .
ENV PYTHONPATH=/app:/app/externals:/app/externals/causal-learn

# Non-root user
RUN useradd -m mcp && chown -R mcp:mcp /app
USER mcp

# Health check
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"

EXPOSE 8000
ENTRYPOINT ["python", "-m", "causal_copilot.mcp", "--http"]
```

**Step 2: Create full Dockerfile**

```dockerfile
# Dockerfile.mcp-full — Full MCP server (~4GB)
# All algorithms including torch-based + LaTeX reports.
FROM pytorch/pytorch:2.2.2-cuda11.8-cudnn8-runtime AS base

WORKDIR /app

# System deps (from existing Dockerfile.cpu)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential graphviz git \
    texlive-latex-base texlive-latex-extra texlive-fonts-recommended && \
    rm -rf /var/lib/apt/lists/*

# Python deps
COPY requirements_cpu.txt .
RUN pip install --no-cache-dir -r requirements_cpu.txt
RUN pip install --no-cache-dir "fastmcp>=3.0"

# Application
COPY . .
ENV PYTHONPATH=/app:/app/externals:/app/externals/causal-learn

# Non-root user
RUN useradd -m mcp && chown -R mcp:mcp /app
USER mcp

HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"

EXPOSE 8000
ENTRYPOINT ["python", "-m", "causal_copilot.mcp", "--http"]
```

**Step 3: Create docker-compose**

```yaml
# docker-compose.mcp.yml
services:
  causal-copilot-mcp:
    build:
      context: .
      dockerfile: Dockerfile.mcp-core
    image: abel/causal-copilot-mcp:latest
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - LLM_PROVIDER=${LLM_PROVIDER:-openai}
      - LLM_MODEL=${LLM_MODEL:-gpt-4o-mini}
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: '2'
```

**Step 4: Update __main__.py for --http flag**

Read `causal_copilot/mcp/__main__.py`, then modify:

```python
# causal_copilot/mcp/__main__.py
"""MCP server entry point."""
import sys

from causal_copilot.mcp.server import mcp

if "--http" in sys.argv:
    mcp.run(transport="streamable-http", host="0.0.0.0", port=8000)
else:
    mcp.run()
```

**Step 5: Create requirements_mcp_core.txt**

```
# Core deps for lightweight MCP server (no torch)
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0
statsmodels>=0.14
openai>=1.0
pydantic>=2.0
fastmcp>=3.0
causal-learn>=0.1.3.7
graphviz>=0.20
lingam>=1.8
```

**Step 6: Test Docker build (manual)**

Run: `docker build -f Dockerfile.mcp-core -t causal-copilot-mcp:test .`
Expected: Build succeeds

**Step 7: Commit**

```bash
git add Dockerfile.mcp-core Dockerfile.mcp-full docker-compose.mcp.yml \
       requirements_mcp_core.txt causal_copilot/mcp/__main__.py
git commit -m "feat: add Docker production setup for MCP server

Three images: core (slim, no torch), full (+torch+LaTeX), gpu.
docker-compose.mcp.yml for one-command startup. HTTP transport on :8000."
```

---

## Phase 3: Distribution + Quality

---

### Task 14: smithery.yaml + Client Configs

**Files:**
- Create: `smithery.yaml`
- Modify: existing README or create `MCP_README.md` (only if user requests)

**Step 1: Create smithery.yaml**

```yaml
# smithery.yaml — MCP registry listing
startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      openaiApiKey:
        type: string
        description: "OpenAI API key for LLM-driven algorithm selection"
      llmProvider:
        type: string
        description: "LLM provider: openai, openrouter, ollama"
        default: "openai"
      offline:
        type: boolean
        description: "Rule-based mode (no LLM needed)"
        default: false
  commandFunction: |
    (config) => ({
      command: 'python',
      args: ['-m', 'causal_copilot.mcp'],
      env: {
        OPENAI_API_KEY: config.openaiApiKey || '',
        LLM_PROVIDER: config.llmProvider || 'openai',
      }
    })
```

**Step 2: Test smithery format**

Run: `python -c "import yaml; yaml.safe_load(open('smithery.yaml'))" && echo OK`
Expected: OK

**Step 3: Commit**

```bash
git add smithery.yaml
git commit -m "feat: add smithery.yaml for MCP registry listing"
```

---

### Task 15: Golden Tests + JSON Schema Snapshots

**Files:**
- Create: `tests/test_mcp_golden.py`
- Create: `tests/golden/` directory with expected schema snapshots

**Step 1: Write golden tests**

```python
# tests/test_mcp_golden.py
"""Golden tests: verify tool output schemas remain stable.

If these fail, it means a tool's output contract changed.
Update the golden snapshot ONLY after verifying the change is intentional.
"""
import json
import numpy as np
import pytest
from unittest.mock import patch, MagicMock


# Schema validators (check required keys, not values)
def _check_keys(result, required_keys):
    """Verify all required keys are present in result."""
    for key in required_keys:
        assert key in result, f"Missing required key: {key}"


class TestOutputSchemaStability:
    """Each tool's output must contain these keys. Adding keys is OK.
    Removing or renaming keys is a breaking change."""

    def test_diagnose_data_schema(self):
        from causal_copilot.mcp.server import diagnose_data
        csv = "x,y\n" + "\n".join(f"{i},{i*2}" for i in range(50))
        result = json.loads(diagnose_data(csv))
        if result["status"] == "ok":
            _check_keys(result, ["status", "diagnosis"])
            _check_keys(result["diagnosis"], [
                "linearity", "data_type", "sample_size", "feature_number",
            ])

    def test_explain_graph_schema(self):
        from causal_copilot.mcp.server import explain_graph
        adj = [[0, 0], [1, 0]]
        names = ["X", "Y"]
        result = json.loads(explain_graph(adj, names))
        _check_keys(result, ["explanation", "graph_stats"])
        _check_keys(result["graph_stats"], [
            "n_directed_edges", "n_undirected_edges",
            "root_causes", "terminal_effects",
        ])

    def test_explain_result_schema(self):
        from causal_copilot.mcp.server import explain_result
        adj = [[0, 0], [1, 0]]
        names = ["X", "Y"]
        result = json.loads(explain_result(adj, names))
        _check_keys(result, ["explanation", "graph_stats", "graph_kind", "identifiability"])

    def test_estimate_effects_error_schema(self):
        from causal_copilot.mcp.server import estimate_effects
        # PAG → should reject
        adj = [[0, 4], [5, 0]]
        names = ["A", "B"]
        csv = "a,b\n" + "\n".join(f"{i},{i*2}" for i in range(50))
        result = json.loads(estimate_effects(
            json.dumps(adj), json.dumps(names), csv,
            treatment="A", outcome="B",
        ))
        _check_keys(result, ["status", "error", "graph_kind"])

    def test_refine_graph_schema(self):
        from causal_copilot.mcp.server import refine_graph
        adj = [[0, 0], [1, 0]]
        names = ["X", "Y"]
        result = json.loads(refine_graph(
            json.dumps(adj), json.dumps(names),
        ))
        _check_keys(result, ["status", "adjacency_matrix", "edges", "graph_kind"])


class TestResourceSchemaStability:
    def test_algorithm_resource_has_required_fields(self):
        from causal_copilot.mcp.resources import get_algorithm_resources
        resources = get_algorithm_resources()
        for r in resources:
            _check_keys(r, ["name", "uri", "description"])

    def test_guide_resources_exist(self):
        from causal_copilot.mcp.resources import get_all_guide_names, get_guide_content
        guides = get_all_guide_names()
        assert "ci-tests" in guides
        assert "score-functions" in guides
        assert "interpreting-graphs" in guides
        for name in guides:
            content = get_guide_content(name)
            assert content is not None
            assert len(content) > 50
```

**Step 2: Run golden tests**

Run: `python -m pytest tests/test_mcp_golden.py -v`
Expected: All PASS

**Step 3: Commit**

```bash
git add tests/test_mcp_golden.py
git commit -m "feat: add golden tests for MCP tool output schema stability

Schema contract tests for all 6 tools + resources. Breaking changes
to output keys will fail these tests."
```

---

### Task 16: Final Integration Test + Server Startup

**Files:**
- Create: `tests/test_mcp_integration.py`
- Modify: `causal_copilot/mcp/server.py` (add health endpoint for HTTP mode)

**Step 1: Write integration test**

```python
# tests/test_mcp_integration.py
"""Integration test: verify MCP server starts and tools are registered."""
import json
import pytest


class TestServerRegistration:
    def test_all_tools_registered(self):
        from causal_copilot.mcp.server import mcp
        tools = mcp._tool_manager._tools
        expected = {"analyze", "diagnose_data", "run_algorithm", "discover",
                    "refine_graph", "estimate_effects", "explain_graph",
                    "explain_result", "list_algorithms"}
        registered = set(tools.keys())
        for t in expected:
            assert t in registered, f"Tool '{t}' not registered"

    def test_prompts_registered(self):
        from causal_copilot.mcp.server import mcp
        prompts = mcp._prompt_manager._prompts
        assert "causal_expert" in prompts or "causal-expert" in prompts

    def test_mcp_cli_entry(self):
        """Verify CLI entry point works."""
        from causal_copilot.cli import main
        with pytest.raises(SystemExit):
            main(["mcp", "--help"])
```

**Step 2: Run integration tests**

Run: `python -m pytest tests/test_mcp_integration.py -v`
Expected: All PASS

**Step 3: Commit**

```bash
git add tests/test_mcp_integration.py
git commit -m "feat: add MCP server integration tests

Verify all 9 tools registered, prompts available, CLI entry point works."
```

---

## Quality Gates

### Gate 0: Pipeline Hardening
- [ ] `python -m pytest tests/test_ci_test_resolver.py tests/test_score_resolver.py tests/test_pdag_policy.py tests/test_resolver_integration.py -v` → 24 PASSED

### Gate 1: MCP Tools Work
- [ ] `python -m pytest tests/test_mcp.py -v` → all existing + new tests PASS
- [ ] `python -m pytest tests/test_mcp_offline.py -v` → 5 PASSED

### Gate 2: Knowledge Layer
- [ ] `python -m pytest tests/test_mcp_resources.py -v` → 4+ PASSED

### Gate 3: Schema Stability
- [ ] `python -m pytest tests/test_mcp_golden.py -v` → 7 PASSED

### Gate 4: Integration
- [ ] `python -m pytest tests/test_mcp_integration.py -v` → 3 PASSED

### Gate 5: Docker
- [ ] `docker build -f Dockerfile.mcp-core -t test .` → succeeds
- [ ] Container starts, /health returns 200

### Full Suite
```bash
python -m pytest tests/test_ci_test_resolver.py tests/test_score_resolver.py \
    tests/test_pdag_policy.py tests/test_resolver_integration.py \
    tests/test_mcp.py tests/test_mcp_bridge.py tests/test_mcp_artifacts.py \
    tests/test_mcp_resources.py tests/test_mcp_offline.py \
    tests/test_mcp_golden.py tests/test_mcp_integration.py -v
```
Expected: ~60 PASSED

---

## Summary

| Phase | Tasks | New Files | Tests |
|-------|-------|-----------|-------|
| 0: Hardening | 4 | 4 (.py) + 4 (tests) | 24 |
| 1: MCP Server | 7 | 4 (.py) + 3 (tests) | ~25 |
| 2: Production | 2 | 4 (Docker/yaml) + 2 (tests) | 10 |
| 3: Quality | 2 | 2 (tests) | 10 |
| **Total** | **15** | **~23 files** | **~69** |

**What this achieves:**
- 6 MCP tools (discover, diagnose_data, run_algorithm, refine_graph, estimate_effects, explain_result) + 3 legacy tools kept for backward compatibility
- 55+ MCP resources (algorithm profiles, HP specs, guides)
- 2 expert prompts
- Data-adaptive CI test + score function resolution
- PDAG policy (DAG/CPDAG/PAG-aware inference)
- Offline rule-based fallback
- Three Docker images
- Smithery registry listing
- ~69 tests covering all tools, schemas, and integration
