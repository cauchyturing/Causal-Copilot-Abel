# Core deps for lightweight MCP server (no torch).
# LLM pipeline falls back to rule-based offline mode without torch.
# Algorithms requiring torch (NOTEARS, GOLEM, etc.) unavailable;
# PC, GES, FCI, DirectLiNGAM, etc. work fine.

# MCP framework (includes uvicorn, python-dotenv, pydantic)
fastmcp>=3.0

# Scientific computing
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.3

# Causal inference (effect estimation)
dowhy>=0.12
econml>=0.15

networkx>=3.0
statsmodels>=0.14

# Causal discovery algorithms
causal-learn>=0.1.3.7
lingam>=1.8

# LLM client (for full pipeline; graceful fallback without API key)
openai>=1.0
